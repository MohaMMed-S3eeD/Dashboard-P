<!DOCTYPE html>
<html lang="ar" dir="rtl">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>إنشاء مستخدم جديد</title>
    <style>
      body { font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif; margin: 0; background: #f7faff; color: #0f172a; }
      .container { max-width: 720px; margin: 24px auto; padding: 0 16px; }
      .card { background: #fff; border: 1px solid #e5e7eb; border-radius: 12px; padding: 20px; }
      .title { margin: 0 0 8px; font-size: 22px; }
      .muted { color: #475569; margin: 0 0 16px; }
      form { display: grid; gap: 12px; }
      label { font-size: 14px; font-weight: 600; }
      input { width: 100%; padding: 10px 12px; border: 1px solid #cbd5e1; border-radius: 8px; font-size: 14px; }
      .row { display: grid; gap: 12px; grid-template-columns: 1fr; }
      .actions { display: flex; gap: 10px; margin-top: 8px; }
      .btn { appearance: none; border: 1px solid #e5e7eb; background: #f8fafc; padding: 10px 14px; border-radius: 8px; cursor: pointer; font-weight: 600; }
      .btn.primary { background: #eef2ff; border-color: #c7d2fe; color: #3730a3; }
      .error { background: #fef2f2; color: #b91c1c; border: 1px solid #fecaca; padding: 10px; border-radius: 8px; margin-bottom: 10px; }
      a { color: #1d4ed8; text-decoration: none; }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="card">
        <h1 class="title">إنشاء مستخدم جديد</h1>
        <p class="muted">أدخل الاسم والبريد الإلكتروني ثم احفظ.</p>
        <% if (typeof error !== 'undefined' && error) { %>
          <div class="error"><%= error %></div>
        <% } %>
        <form action="/users" method="post" novalidate>
          <div class="row">
            <div>
              <label for="name">الاسم</label>
              <input id="name" name="name" type="text" required value="<%= typeof name !== 'undefined' ? name : '' %>" />
            </div>
            <div>
              <label for="email">البريد الإلكتروني</label>
              <input id="email" name="email" type="email" required value="<%= typeof email !== 'undefined' ? email : '' %>" />
            </div>
          </div>
          <div class="actions">
            <button class="btn primary" type="submit">حفظ</button>
            <a class="btn" href="/">الرجوع</a>
            <a class="btn" href="/api/users-file" target="_blank" rel="noopener">عرض ملف المستخدمين</a>
          </div>
        </form>
      </div>
    </div>
  </body>
  </html>


